<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :filename, class: "control-label" %>
    <%= text_input f, :filename, class: "form-control" %>
    <%= error_tag f, :filename %>
  </div>

  <div class="form-group">
    <%= label f, :activity_id, class: "control-label" %>
    <%= number_input f, :activity_id, class: "form-control" %>
    <%= error_tag f, :activity_id %>
  </div>

  <div class="form-group">
    <%= label f, :thumbnail, class: "control-label" %>
    <%= text_input f, :thumbnail, class: "form-control" %>
    <%= error_tag f, :thumbnail %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
